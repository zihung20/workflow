// @monaco-editor/react loads Monaco from CDN by default, which bundles
// the TypeScript language service including the ts.worker. No custom
// worker setup needed — this file intentionally left minimal.
// Type declarations for flowyd / zod are registered in monacoSetup.ts
// via the CodeEditor component's beforeMount callback.
